//it's load every picture for everyeach products
function LoadPictureForItems(ProdID, ProductWithphotos) {
    //ProductWithphotos.forEach(function (element) {
    //    console.log(element);
    //});

}